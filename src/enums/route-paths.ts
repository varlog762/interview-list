export enum RoutePaths {
  ROOT = '/',
  ADD = '',
  INTERVIEWS = 'interviews',
  STATISTICS = 'statistics',
  AUTH = 'auth',
  LOGIN = 'login',
  REGISTER = 'register',
  NOT_FOUND = '/:catchAll(.*)*',
}
