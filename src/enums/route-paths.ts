export enum RoutePaths {
  ROOT = '/',
  ADD = 'add',
  INTERVIEWS = 'interviews',
  STATISTICS = 'statistics',
  AUTH = '/auth',
  LOGIN = 'login',
  REGISTER = 'register',
  NOT_FOUND = '/:catchAll(.*)*',
}
